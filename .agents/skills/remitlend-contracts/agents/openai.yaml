interface:
  display_name: "Remitlend Contracts"
  short_description: "Repo-specific patterns and workflows for remitlend-contracts"
  default_prompt: "Use the remitlend-contracts repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true